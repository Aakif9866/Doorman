GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
MONGO_URI=mongodb://localhost:27017/doorman
PORT=4000

# Optional. If set, /api/candidates and /api/samples require this value in
# an x-api-key header. Unset = unauthenticated (fine for local/demo use only).
API_KEY=

# Optional. Only needed if you run a separate frontend dev server on another
# origin/port. The bundled client/index.html is served same-origin and needs
# this unset.
ALLOWED_ORIGIN=
